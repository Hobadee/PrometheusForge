overrides:
  - selector:
      tags: ['build']
    settings:
      retryPolicy:
        type: 'retry'
        attempts: 5
  - selector:
      ids: ['task.test']
    settings:
      plugins:
        textOutput:
          outputFormat: 'Json'
